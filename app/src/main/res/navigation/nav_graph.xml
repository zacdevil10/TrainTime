<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/arrivals_fragment">

    <fragment
        android:id="@+id/arrivals_fragment"
        android:label="Arrivals"
        android:name="uk.co.zac_h.traintime.ui.arrivals.ArrivalsFragment"
        tools:layout="@layout/arrivals_fragment">
        <action
            android:id="@+id/action_arrivals_fragment_to_line_fragment"
            app:enterAnim="@android:anim/fade_in"
            app:exitAnim="@android:anim/fade_out"
            app:popEnterAnim="@android:anim/fade_in"
            app:popExitAnim="@android:anim/fade_out"
            app:destination="@id/line_fragment" />
    </fragment>

    <fragment
        android:id="@+id/line_fragment"
        android:label="Train Line"
        android:name="uk.co.zac_h.traintime.ui.lines.TrainLineFragment"
        tools:layout="@layout/train_line_fragment"/>

</navigation>